# URL base do backend FastAPI (server/) que este app web vai consumir.
# Pode ser ajustado depois direto pela tela de login/config do app (fica salvo no navegador).
VITE_API_BASE_URL=http://10.136.59.60:8766
